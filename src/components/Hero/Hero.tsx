import React from 'react';
import style from "./Hero.module.css"
import { Navbar } from '..';

const Hero = () => {
    return (
        <section className={style.hero}>
            {/* <Navbar /> */}
Hero
        </section>
    );
}

export default Hero;